[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra

# Rationale:
# This repo includes several root-level `test_*.py` developer scripts that execute
# at import time (not pytest-style tests). Pytest will try to collect/import
# them by default, which can cause side effects and break test runs.
